---
on:
  pull_request:
    paths:
      - '**/*.go'
      - go.mod
      - go.sum

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

name: build
jobs:
  # The job key names the check run, which is what the repository ruleset
  # requires by name ("build (1.26.x)"). Four workflows used to call this job
  # `golang`, so all four reported the same indistinguishable check and none of
  # the required names ever arrived.
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        go-version: ['1.26.x']
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Install Go
        uses: actions/setup-go@v6
        with:
          go-version: ${{ matrix.go-version }}
          cache: true
          cache-dependency-path: go.sum

      - name: Build
        run: make build
