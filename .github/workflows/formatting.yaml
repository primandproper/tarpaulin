---
on:
  pull_request:
    paths:
      - '**/*.go'

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

name: formatting
jobs:
  # The job key names the check run the ruleset requires; see build.yaml.
  formatting:
    timeout-minutes: 10
    runs-on: ubuntu-latest
    strategy:
      matrix:
        go-version: ['1.26.x']
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Install Go
        uses: actions/setup-go@v6
        with:
          go-version: ${{ matrix.go-version }}
          cache: true
          cache-dependency-path: go.sum

      # `gofmt -l` walks the filesystem rather than Go's package wildcard, so
      # unlike `go vet ./...` it has to be told which files are the module's.
      # scripts/go_files.sh is the one place that answers, here and locally, by
      # asking the toolchain — which is what keeps vendor/ and testdata/ out.
      # testdata/ has to stay out: it is the analyzer's fixture corpus,
      # deliberately unformatted, deliberately unparseable in places, and pinned
      # to exact line numbers.
      - name: check formatting
        run: |
          # pipefail matters more than usual here: without it, go_files.sh
          # failing mid-pipeline is masked by xargs exiting 0 on no input, and
          # the step passes having checked nothing at all.
          set -euo pipefail

          unformatted=$(scripts/go_files.sh . | xargs -0 gofmt -l)

          if [ -n "${unformatted}" ]; then
            echo "::error::gofmt reports these files as unformatted:"
            echo "${unformatted}"
            exit 1
          fi
